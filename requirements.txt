# Python dependencies (pip).
# Apt-only (not on PyPI): SoapySDR, gnuradio, hackrf, rtl-sdr — see scripts/install_deps.sh
# ML/CV: pip install -r requirements-ml.txt
scapy
pyserial
numpy
scipy
cryptography
paho-mqtt
pymavlink
pyyaml
flask
esptool
flask-socketio
requests
paramiko
